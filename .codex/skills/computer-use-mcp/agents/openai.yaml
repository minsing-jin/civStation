interface:
  display_name: "Computer Use MCP"
  short_description: "Layered MCP workflows for context, strategy, memory, action, and HITL"
  brand_color: "#0F766E"
  default_prompt: "Use the layered computer-use MCP server and prefer the smallest set of tools that solves the task"
policy:
  allow_implicit_invocation: true
